export default function FilterButton({ filterOptionText }) {
    return (
        <button className="text-gray-600 bg-transparent py-1 text-left hover:text-blue-300">{filterOptionText}</button>
    )
}
